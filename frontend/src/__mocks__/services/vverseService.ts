const vverseService = {
  fetchPosts: jest.fn(),
  createPost: jest.fn(),
  likePost: jest.fn(),
  addComment: jest.fn(),
  deletePost: jest.fn(),
  updatePost: jest.fn()
};

export default vverseService;